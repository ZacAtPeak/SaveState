# Deferred Items (08-02)

- `apps/dm_app` test failures observed during plan-level verification are out-of-scope for this plan's file set and appear pre-existing in widget lifecycle (`WikiProvider used after disposed`) and integration expectations (`WikiProvider` not found in `wiki_entry_integration_test`).
- Verification output captured during `flutter test` run in `apps/dm_app` on 2026-05-08.
