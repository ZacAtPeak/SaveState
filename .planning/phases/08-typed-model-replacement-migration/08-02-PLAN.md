---
phase: 08-typed-model-replacement-migration
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/core/test/wiki_page_string_type_test.dart
  - packages/core/lib/data/demo_entities.dart
  - packages/core/lib/data/demo_player_characters.dart
  - packages/core/lib/data/demo_monsters.dart
  - packages/core/lib/data/demo_npcs.dart
  - packages/core/lib/data/data.dart
autonomous: true
requirements: [MIGRATE-01]
must_haves:
  truths:
    - "D-09: Core demo data source is unified as GameEntity payloads (`demoEntities`), not typed-class lists."
    - "D-10: Unified demo entity source preserves prior typed-model fields needed by D&D behavior."
    - "D-11: Former enum-like values are stored as schema-key strings aligned with GameModel conventions."
    - "D-12: Complex mechanics structures remain nested maps/lists and are not flattened to display strings."
    - "D-14: Demo helper splits provide character/monster/npc groupings for downstream DM consumption."
  artifacts:
    - path: "packages/core/lib/data/demo_entities.dart"
      provides: "Unified demoEntities list using GameEntity payloads"
    - path: "packages/core/test/wiki_page_string_type_test.dart"
      provides: "Automated assertions for strict key serialization and unified demo helper contracts"
  key_links:
    - from: "demo_entities.dart"
      to: "dm_app main sidebar sections"
      via: "pre-split helper filters"
      pattern: "demoEntities.*where"
    - from: "data.dart exports"
      to: "downstream app imports"
      via: "barrel export includes demo_entities.dart"
      pattern: "export 'demo_entities.dart'"
---

<objective>
Migrate demo model source from typed classes to unified `GameEntity` payloads and keep DM app behavior functional via direct map-read bridge with safe defaults.

Purpose: satisfy typed-model replacement path while preserving current D&D UX in Phase 8.
Output: unified demo data + schema-contract tests for migrated entity-backed seed content.
</objective>

<execution_context>
@/Users/zacharyreyes/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/zacharyreyes/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-typed-model-replacement-migration/08-CONTEXT.md
@.planning/phases/08-typed-model-replacement-migration/08-PATTERNS.md

<interfaces>
From packages/core/lib/models/game_entity.dart:
```dart
class GameEntity {
  final String entityTypeKey;
  Map<String, dynamic> toJson();
  factory GameEntity.fromJson(Map<String, dynamic> json);
  int getInt(String key, {int fallback = 0});
  String getString(String key, {String fallback = ''});
  bool getBool(String key, {bool fallback = false});
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for strict wiki key serialization and GameEntity demo contracts</name>
  <files>packages/core/test/wiki_page_string_type_test.dart</files>
  <read_first>
    - packages/core/test/wiki_page_test.dart
  </read_first>
  <behavior>
    - WikiPage toJson includes `entityTypeKey` and excludes legacy `pageType`.
    - Demo entity helpers produce character/monster/npc splits from unified `demoEntities`.
    - Demo payload keeps nested map/list mechanics structures intact (per D-12).
  </behavior>
  <action>Create focused core tests that lock post-migration schema contract and unified demo entity helper behavior before deleting typed demo lists.</action>
  <verify>
    <automated>cd packages/core && dart test test/wiki_page_string_type_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `rg "entityTypeKey" packages/core/test/wiki_page_string_type_test.dart` returns assertions.
    - `rg "pageType" packages/core/test/wiki_page_string_type_test.dart` finds only negative assertion/use in fixture legacy case.
    - `rg "demoCharacterEntities|demoMonsterEntities|demoNpcEntities" packages/core/test/wiki_page_string_type_test.dart` finds helper assertions.
  </acceptance_criteria>
  <done>Tests encode expected contracts for migrated wiki key serialization and unified demo entity source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement unified demoEntities and pre-split section helpers</name>
  <files>packages/core/lib/data/demo_entities.dart, packages/core/lib/data/demo_player_characters.dart, packages/core/lib/data/demo_monsters.dart, packages/core/lib/data/demo_npcs.dart, packages/core/lib/data/data.dart</files>
  <read_first>
    - packages/core/lib/data/demo_player_characters.dart
    - packages/core/lib/data/demo_monsters.dart
    - packages/core/lib/data/demo_npcs.dart
    - packages/core/lib/data/data.dart
  </read_first>
  <behavior>
    - Data source is one unified `demoEntities` list (per D-09).
    - Payload preserves prior fields and nested structures (per D-10, D-12).
    - Enum-like values are stored as schema-key strings (per D-11).
  </behavior>
  <action>Create `demo_entities.dart` exporting `demoEntities` plus pre-split helpers (`demoCharacterEntities`, `demoMonsterEntities`, `demoNpcEntities`) for DM sidebar usage per D-14; migrate all existing typed demo payloads to GameEntity with D&D key names unchanged.</action>
  <verify>
    <automated>cd packages/core && dart test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "final demoEntities = <GameEntity>" packages/core/lib/data/demo_entities.dart` finds unified list.
    - `rg "demoCharacterEntities|demoMonsterEntities|demoNpcEntities" packages/core/lib/data/demo_entities.dart` finds pre-split helpers.
    - `rg "export 'demo_entities.dart'" packages/core/lib/data/data.dart` passes.
  </acceptance_criteria>
  <done>Core demo data is fully GameEntity-backed and section-splittable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core demo data → DM UI bridge | Dynamic map values cross into typed widget DTOs |
| GameEntity accessors → UI calculations | Invalid/missing keys could disrupt rendering and initiative calculations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | D | DM conversion factories | mitigate | Use GameEntity accessor fallbacks for all optional/missing fields and cover with smoke tests (ASVS V5 defensive input handling). |
| T-08-05 | T | Demo migration payload mapping | mitigate | Preserve legacy field keys exactly and assert section splits/critical fields in tests to prevent silent data drift. |
| T-08-06 | I | UI bridge logging | accept | No sensitive data in local demo fixtures; warnings remain local-only and non-PII. |
</threat_model>

<verification>
- `cd packages/core && dart test`
- `cd apps/dm_app && flutter test`
</verification>

<success_criteria>
- Unified `demoEntities` powers DM sidebar and tracker.
- D&D behavior is preserved with fallback-safe rendering.
- No typed demo model dependency remains in DM app flow.
</success_criteria>

<output>
After completion, create `.planning/phases/08-typed-model-replacement-migration/08-02-SUMMARY.md`
</output>
