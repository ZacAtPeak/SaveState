---
phase: 08-typed-model-replacement-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/test/wiki_migration_runner_test.dart
  - packages/core/lib/migrations/wiki_migration_runner.dart
  - packages/core/lib/models/wiki_page.dart
  - packages/core/lib/services/wiki_storage_service.dart
  - packages/core/lib/wiki/wiki_provider.dart
  - apps/dm_app/lib/main.dart
  - apps/companion_app/lib/main.dart
autonomous: true
requirements: [MIGRATE-02]
must_haves:
  truths:
    - "D-01: Existing wiki JSON files using legacy pageType values are rewritten to entityTypeKey before wiki pages load."
    - "D-02: Runtime wiki deserialization uses entityTypeKey-only parsing after migration completes."
    - "D-03: Unknown legacy type files are skipped with warnings and do not crash migration/startup."
    - "D-04: Migration rewrites wiki files in place (no backup/parallel output directory)."
    - "D-05: WikiMigrationRunner executes before normal wiki page loading on app startup."
    - "D-06: Startup migration is idempotent and runs each launch without marker gating."
    - "D-07: Write failures surface warnings while startup continues without hard block."
    - "D-08: Migration scan scope is limited to persisted wiki page files under wiki/pages."
  artifacts:
    - path: "packages/core/lib/migrations/wiki_migration_runner.dart"
      provides: "Idempotent in-place wiki JSON migration scoped to wiki/pages"
    - path: "packages/core/lib/models/wiki_page.dart"
      provides: "Strict entityTypeKey serialization/deserialization"
    - path: "packages/core/test/wiki_migration_runner_test.dart"
      provides: "Automated migration safety coverage"
  key_links:
    - from: "apps/*/lib/main.dart"
      to: "WikiMigrationRunner"
      via: "startup invocation before WikiProvider.loadAll"
      pattern: "migrate.*before.*loadAll"
    - from: "wiki_migration_runner.dart"
      to: "wiki/pages/*.json"
      via: "scan/parse/rewrite"
      pattern: "path.join\(.*wiki.*pages"
---

<objective>
Deliver the Phase 8 migration gate: rewrite persisted wiki JSON from legacy `pageType` to canonical `entityTypeKey` before any normal wiki load, with strict post-migration parsing.

Purpose: prevent runtime breakage while deleting `WikiPageType` and preserve existing user data.
Output: migration runner + startup wiring + strict wiki model + tests.
</objective>

<execution_context>
@/Users/zacharyreyes/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/zacharyreyes/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-typed-model-replacement-migration/08-CONTEXT.md
@.planning/phases/08-typed-model-replacement-migration/08-RESEARCH.md
@.planning/phases/08-typed-model-replacement-migration/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing migration tests for legacy wiki JSON rewrite</name>
  <files>packages/core/test/wiki_migration_runner_test.dart</files>
  <read_first>
    - packages/core/test/wiki_storage_service_test.dart
    - packages/core/lib/services/wiki_storage_service.dart
  </read_first>
  <behavior>
    - Legacy file with {"pageType":"creature"} is rewritten to {"entityTypeKey":"creature"} and no pageType key remains.
    - Unknown legacy type value is skipped with warning count increment (per D-03), migration does not throw.
    - Running migration twice produces same file content and same migrated count on second run = 0 (per D-06).
  </behavior>
  <action>Create temp-dir based tests that build `wiki/pages/*.json` fixtures, invoke `WikiMigrationRunner`, and assert exact JSON key changes, skip behavior, and idempotency per D-01/D-03/D-06.</action>
  <verify>
    <automated>cd packages/core && dart test test/wiki_migration_runner_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `dart test test/wiki_migration_runner_test.dart` passes.
    - `rg "entityTypeKey" packages/core/test/wiki_migration_runner_test.dart` finds explicit assertions.
    - `rg "unknown|skip|warning" packages/core/test/wiki_migration_runner_test.dart` finds the unknown-type case.
  </acceptance_criteria>
  <done>Tests fail before runner implementation and pass after implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement WikiMigrationRunner + strict WikiPage key schema</name>
  <files>packages/core/lib/migrations/wiki_migration_runner.dart, packages/core/lib/models/wiki_page.dart</files>
  <read_first>
    - packages/core/lib/models/wiki_page.dart
    - packages/core/lib/services/wiki_storage_service.dart
    - packages/core/assets/game_models/dnd5e.json
  </read_first>
  <behavior>
    - Serialization emits `entityTypeKey` only (per D-01, D-02).
    - Deserialization reads `entityTypeKey` only and throws readable `FormatException` if missing.
    - Runner scans only `wiki/pages`, rewrites in place, ignores malformed files, and continues on write error with warnings (per D-04/D-07/D-08).
  </behavior>
  <action>Create `WikiMigrationRunner` with mapping for legacy values (`creature`, `spell`, `item`, `rule`, `location`, `npc`, `other`) to D&D entity keys, perform in-place JSON rewrite per D-04, and update `WikiPage` model fields/mapping from `pageType` to `entityTypeKey` with strict post-migration parse per D-02.</action>
  <verify>
    <automated>cd packages/core && dart test test/wiki_migration_runner_test.dart && dart test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "entityTypeKey" packages/core/lib/models/wiki_page.dart` returns toJson/fromJson usage.
    - `rg "pageType" packages/core/lib/models/wiki_page.dart` returns no JSON key usage.
    - `rg "wiki.*pages" packages/core/lib/migrations/wiki_migration_runner.dart` confirms scan scope.
  </acceptance_criteria>
  <done>Wiki JSON compatibility is migrated and strict parsing path is in place.</done>
</task>

<task type="auto">
  <name>Task 3: Run migration at startup before first wiki load in both apps</name>
  <files>packages/core/lib/services/wiki_storage_service.dart, packages/core/lib/wiki/wiki_provider.dart, apps/dm_app/lib/main.dart, apps/companion_app/lib/main.dart</files>
  <read_first>
    - packages/core/lib/wiki/wiki_provider.dart
    - apps/dm_app/lib/main.dart
    - apps/companion_app/lib/main.dart
  </read_first>
  <action>Wire migration execution before `WikiProvider.loadAll()` (per D-05), call it every startup without marker gating (per D-06), and keep startup non-blocking on migration write failures with warning logging (per D-07).</action>
  <verify>
    <automated>cd packages/core && dart test test/wiki_migration_runner_test.dart && cd ../../apps/dm_app && flutter test test/game_entity_sidebar_smoke_test.dart</automated>
    <automated>cd packages/core && dart test && cd ../../apps/companion_app && flutter test && cd ../dm_app && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "loadAll\(" apps/dm_app/lib/main.dart apps/companion_app/lib/main.dart` still shows wiki load path.
    - `rg "migrat" apps/dm_app/lib/main.dart apps/companion_app/lib/main.dart packages/core/lib/wiki/wiki_provider.dart` shows startup migration invocation.
    - Both app test suites pass.
  </acceptance_criteria>
  <done>Both apps perform migration before normal wiki reads and remain launchable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file system → migration parser | Untrusted JSON content crosses into parsing and rewrite logic |
| migration runner → startup flow | Migration errors can impact app availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | D | WikiMigrationRunner parse loop | mitigate | Catch per-file decode/type errors, skip file, continue; assert with migration tests (ASVS V5). |
| T-08-02 | T | In-place rewrite path | mitigate | Validate map shape before write and only rewrite targeted keys in `wiki/pages` scope; do not rewrite arbitrary paths. |
| T-08-03 | D | Startup migration integration | mitigate | Non-blocking startup on migration write failure (warning path), no hard crash; verify launch tests in both apps. |
</threat_model>

<verification>
- `cd packages/core && dart test`
- `cd apps/companion_app && flutter test`
- `cd apps/dm_app && flutter test`
</verification>

<success_criteria>
- Legacy wiki files are rewritten to `entityTypeKey` before load.
- Unknown/malformed files are skipped with warnings, not crashes.
- Strict `WikiPage` schema uses new key only.
</success_criteria>

<output>
After completion, create `.planning/phases/08-typed-model-replacement-migration/08-01-SUMMARY.md`
</output>
