---
phase: 08-typed-model-replacement-migration
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - packages/core/lib/models/player_character.dart
  - packages/core/lib/models/monster.dart
  - packages/core/lib/models/npc.dart
  - packages/core/lib/models/wiki_page_type.dart
  - packages/core/lib/models/enums.dart
  - packages/core/lib/models/models.dart
  - packages/core/lib/models/item.dart
  - packages/core/lib/models/value_types.dart
autonomous: true
requirements: [MIGRATE-03]
must_haves:
  truths:
    - "Legacy typed model and enum files are removed, and workspace compiles cleanly."
    - "Remaining core model parsing uses String/GameModel-aligned values without enum abstractions."
    - "Both apps and core tests stay green after destructive cleanup."
  artifacts:
    - path: "packages/core/lib/models/models.dart"
      provides: "Barrel exports without deleted typed model/enum files"
    - path: "packages/core/lib/models/item.dart"
      provides: "String-backed type/value parsing replacing enum dependencies"
    - path: "packages/core/lib/models/value_types.dart"
      provides: "String-backed value type logic replacing enum dependencies"
  key_links:
    - from: "models.dart"
      to: "core/app imports"
      via: "barrel export cleanup"
      pattern: "export '.*'"
    - from: "item.dart/value_types.dart"
      to: "runtime parsing"
      via: "string-based parse/serialize"
      pattern: "String"
---

<objective>
Finalize destructive cleanup by deleting legacy typed model and enum files after migration + bridge rewiring are complete.

Purpose: satisfy MIGRATE-03 without regressions and complete irreversible move to GameEntity/GameModel-aligned string semantics.
Output: deleted legacy files + compile-clean replacements + full regression verification.
</objective>

<execution_context>
@/Users/zacharyreyes/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/zacharyreyes/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-typed-model-replacement-migration/08-CONTEXT.md
@.planning/phases/08-typed-model-replacement-migration/08-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete typed model and enum files, clean model barrel exports</name>
  <files>packages/core/lib/models/player_character.dart, packages/core/lib/models/monster.dart, packages/core/lib/models/npc.dart, packages/core/lib/models/wiki_page_type.dart, packages/core/lib/models/enums.dart, packages/core/lib/models/models.dart</files>
  <read_first>
    - packages/core/lib/models/models.dart
  </read_first>
  <action>Delete `player_character.dart`, `monster.dart`, `npc.dart`, `wiki_page_type.dart`, and `enums.dart`, then remove all corresponding exports from `models.dart` so no deleted symbols remain importable.</action>
  <verify>
    <automated>cd packages/core && dart analyze</automated>
  </verify>
  <done>Legacy typed model and enum files are removed and barrel exports no longer reference them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace enum-dependent parsing in item/value_types with String-backed semantics</name>
  <files>packages/core/lib/models/item.dart, packages/core/lib/models/value_types.dart</files>
  <read_first>
    - packages/core/lib/models/item.dart
    - packages/core/lib/models/value_types.dart
  </read_first>
  <behavior>
    - Previous enum-backed values are parsed/serialized as strings aligned to GameModel conventions.
    - Existing consumers remain compile-compatible after enum deletion.
    - Unknown string values use safe defaults rather than throwing during normal parse flows.
  </behavior>
  <action>Refactor `item.dart` and `value_types.dart` to remove enum dependencies and use explicit String-based parse/serialize logic with safe defaults, preserving current field behavior expected by tests.</action>
  <verify>
    <automated>cd packages/core && dart test</automated>
  </verify>
  <done>MIGRATE-03 enum replacement is complete and core tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run cross-workspace regression verification after destructive cleanup</name>
  <files>packages/core/lib/models/models.dart, packages/core/lib/models/item.dart, packages/core/lib/models/value_types.dart</files>
  <action>Run full regression sweep to confirm typed-model/enum deletion did not break core logic or either app package.</action>
  <verify>
    <automated>cd packages/core && dart analyze && dart test test/wiki_page_string_type_test.dart</automated>
    <automated>cd packages/core && dart test && cd ../../apps/companion_app && flutter test && cd ../dm_app && flutter test</automated>
  </verify>
  <done>All package test suites are green with deleted files absent and no enum-era compile dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enum/type deletion → app/runtime parse logic | Removed symbols can cause compile/runtime regression if any dependency remains |
| string parse defaults → domain logic | Invalid values could silently degrade behavior if defaults are unsafe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | D | Compile/runtime after deletion | mitigate | Enforce analyzer + full workspace tests immediately after file deletion and parsing refactor. |
| T-08-11 | T | String parse replacement | mitigate | Use explicit parse maps/defaults in `item.dart` and `value_types.dart`; verify via core tests. |
| T-08-12 | E | Residual imports of deleted symbols | mitigate | Remove barrel exports and validate zero references by analyzer before final verification. |
</threat_model>

<verification>
- `cd packages/core && dart analyze`
- `cd packages/core && dart test`
- `cd apps/companion_app && flutter test`
- `cd apps/dm_app && flutter test`
</verification>

<success_criteria>
- `PlayerCharacter`, `Monster`, `NPC`, `WikiPageType`, and `enums.dart` are deleted.
- No compile/runtime dependency on enum-era symbols remains.
- Both apps and core tests pass after cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/08-typed-model-replacement-migration/08-04-SUMMARY.md`
</output>
