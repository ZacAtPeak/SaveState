---
phase: 08-typed-model-replacement-migration
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - packages/core/lib/wiki/wiki_modal_provider.dart
  - packages/core/lib/wiki/wiki_type_picker.dart
  - packages/core/lib/services/wiki_storage_service.dart
  - packages/core/lib/wiki/wiki_create_form.dart
  - packages/core/lib/wiki/wiki_provider.dart
  - apps/dm_app/lib/main.dart
  - apps/dm_app/lib/widgets/initiative_tracker.dart
  - apps/dm_app/lib/widgets/creature_detail_view.dart
  - apps/dm_app/test/game_entity_sidebar_smoke_test.dart
autonomous: true
requirements: [MIGRATE-01, MIGRATE-02]
must_haves:
  truths:
    - "D-13: DM app demo sidebar and encounter entry flow are sourced from direct GameEntity map reads, not typed classes or adapter-primary flow."
    - "D-14: DM sidebar grouping remains character/monster/npc via pre-split data-layer helpers despite unified storage."
    - "D-15: Initiative behavior remains current D&D-style drop roll (`d20 + DEX`) in this phase."
    - "D-16: Missing expected D&D fields use safe fallback defaults and do not crash DM widgets."
    - "D-02: Wiki create flow still works using strict entityTypeKey-only runtime paths after fallback removals."
  artifacts:
    - path: "apps/dm_app/lib/widgets/initiative_tracker.dart"
      provides: "GameEntity-based combatant conversion with DEX fallback and D-15 initiative behavior"
    - path: "apps/dm_app/lib/widgets/creature_detail_view.dart"
      provides: "GameEntity-based detail conversion with safe fallbacks"
    - path: "packages/core/lib/wiki/wiki_type_picker.dart"
      provides: "EntityType-only picker path (no enum fallback)"
    - path: "packages/core/lib/services/wiki_storage_service.dart"
      provides: "Submit/storage paths free of WikiPageType references"
  key_links:
    - from: "demo_entities.dart"
      to: "dm_app main sidebar sections"
      via: "pre-split helper filters"
      pattern: "demoEntities.*where"
    - from: "wiki create flow"
      to: "submitFromSchema"
      via: "entity schema based submit"
      pattern: "submitFromSchema"
---

<objective>
Complete DM GameEntity bridge rewiring and remove enum-era wiki fallback paths after migration gate completion.

Purpose: satisfy D-13 through D-16 while preserving wiki create flow on strict entity-key runtime path.
Output: DM bridge rewiring + fallback-free wiki flow + regression verification.
</objective>

<execution_context>
@/Users/zacharyreyes/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/zacharyreyes/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-typed-model-replacement-migration/08-CONTEXT.md
@.planning/phases/07-provider-rewiring/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove enum-era fallback paths from wiki flow</name>
  <files>packages/core/lib/wiki/wiki_modal_provider.dart, packages/core/lib/wiki/wiki_type_picker.dart, packages/core/lib/services/wiki_storage_service.dart, packages/core/lib/wiki/wiki_create_form.dart, packages/core/lib/wiki/wiki_provider.dart</files>
  <read_first>
    - packages/core/lib/wiki/wiki_modal_provider.dart
    - packages/core/lib/wiki/wiki_type_picker.dart
    - packages/core/lib/services/wiki_storage_service.dart
    - packages/core/lib/wiki/wiki_provider.dart
  </read_first>
  <action>Delete deprecated `pendingType` compatibility path and `WikiPageType` enum fallback branches in picker/submit flow, making entity-key/schema path the only runtime path (per D-02 and migration completion from 08-01).</action>
  <verify>
    <automated>cd packages/core && dart analyze && dart test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pendingType|WikiPageType\.values|_entityFromPageType" packages/core/lib/wiki packages/core/lib/services/wiki_storage_service.dart` returns no matches.
    - `rg "pendingEntityKey|submitFromSchema" packages/core/lib/wiki packages/core/lib/services/wiki_storage_service.dart` returns matches.
  </acceptance_criteria>
  <done>Wiki flow is fully entity-key driven with no enum fallback residue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire DM app bridge factories to GameEntity with safe defaults</name>
  <files>apps/dm_app/lib/main.dart, apps/dm_app/lib/widgets/initiative_tracker.dart, apps/dm_app/lib/widgets/creature_detail_view.dart, apps/dm_app/test/game_entity_sidebar_smoke_test.dart</files>
  <read_first>
    - apps/dm_app/lib/main.dart
    - apps/dm_app/lib/widgets/initiative_tracker.dart
    - apps/dm_app/lib/widgets/creature_detail_view.dart
    - packages/core/lib/data/demo_entities.dart
  </read_first>
  <behavior>
    - DM bridge reads GameEntity maps directly (per D-13).
    - Sidebar remains sectioned character/monster/npc via helpers (per D-14).
    - Initiative remains d20 + DEX in this phase (per D-15) and missing fields use fallback values (per D-16).
  </behavior>
  <action>Replace `fromPlayerCharacter/fromMonster/fromNPC` factory call sites with `fromGameEntity` conversions, map D&D keys explicitly (`name`, `hitPoints`, `armorClass`, `dexterity`, etc.), and apply fallback defaults to prevent runtime exceptions per D-16.</action>
  <verify>
    <automated>cd apps/dm_app && flutter test test/game_entity_sidebar_smoke_test.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "fromGameEntity" apps/dm_app/lib/widgets/initiative_tracker.dart apps/dm_app/lib/widgets/creature_detail_view.dart` finds new factories.
    - `rg "demoPlayerCharacters|demoMonsters|demoNPCs" apps/dm_app/lib/main.dart` returns no references.
    - `flutter test` passes for dm_app.
  </acceptance_criteria>
  <done>DM app operates on GameEntity-backed demos without typed model inputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-app launch and wiki create flow verification after fallback removal + DM bridge rewiring</name>
  <read_first>
    - apps/dm_app/lib/main.dart
    - apps/companion_app/lib/main.dart
    - packages/core/lib/wiki/wiki_modal_shell.dart
  </read_first>
  <what-built>Fallback-free wiki flow with GameEntity-backed DM bridge.</what-built>
  <how-to-verify>
    1. Run `cd apps/dm_app && flutter run` and confirm app launches to DM home.
    2. Open wiki modal, create one wiki page from type picker, submit, and confirm it appears in list.
    3. Run `cd ../companion_app && flutter run` and confirm companion app launches and wiki modal opens.
    4. Re-open DM app and verify previously created wiki page still loads (validates migrated storage path).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or provide failing step + observed behavior.</resume-signal>
  <verify>
    <automated>cd apps/dm_app && flutter test test/game_entity_sidebar_smoke_test.dart && cd ../companion_app && flutter test</automated>
    <automated>cd packages/core && dart test && cd ../../apps/companion_app && flutter test && cd ../dm_app && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/core && dart test` and both app `flutter test` commands are green before manual launch checks.
    - Human confirms both apps launch and wiki create flow succeeds end-to-end.
  </acceptance_criteria>
  <done>Post-migration runtime behavior is functionally accepted in real app runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| post-migration code paths → persisted files | Strict parser now trusts only canonical keys |
| enum/type deletion → app startup/runtime | Compile/runtime regressions can affect availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | D | Wiki fallback removal regressions | mitigate | Enforce full workspace automated test sweep plus human launch verification before acceptance. |
| T-08-08 | T | Residual enum fallback branches | mitigate | Remove fallback code and grep-gate zero matches for deleted enum symbols in wiki flow. |
| T-08-09 | E | Unsafe type coercion in DM bridge | mitigate | Use explicit GameEntity key lookups with defaults, validated via dm_app tests. |
</threat_model>

<verification>
- `cd packages/core && dart test`
- `cd apps/companion_app && flutter test`
- `cd apps/dm_app && flutter test`
- Manual app launch + wiki create flow verification
</verification>

<success_criteria>
- DM app flow uses GameEntity-backed entities with safe fallback defaults.
- No compile/runtime dependency on enum-era fallback paths remains in wiki create path.
- Both apps and wiki create flow function after rewiring.
</success_criteria>

<output>
After completion, create `.planning/phases/08-typed-model-replacement-migration/08-03-SUMMARY.md`
</output>
