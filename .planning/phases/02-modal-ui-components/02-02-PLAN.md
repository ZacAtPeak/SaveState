---
phase: 02-modal-ui-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/lib/wiki/wiki_page_list.dart
autonomous: true
requirements: [MODAL-03, LIST-02, LIST-03]
must_haves:
  truths:
    - "Sidebar shows scrollable list of wiki pages"
    - "Each list item shows type indicator (icon) and type name (chip)"
    - "Search bar at top of sidebar filters results as user types"
  artifacts:
    - path: "packages/core/lib/wiki/wiki_page_list.dart"
      provides: "Scrollable page list with type indicators and search bar"
      exports: ["WikiPageList"]
  key_links:
    - from: "packages/core/lib/wiki/wiki_page_list.dart"
      to: "WikiPageTypeExtension.displayName"
      via: "Chip label text"
      pattern: "page\\.pageType\\.displayName"
    - from: "packages/core/lib/wiki/wiki_page_list.dart"
      to: "TextField.onChanged"
      via: "search query callback"
      pattern: "onChanged.*onQueryChanged"
---

<objective>
Create the WikiPageList sidebar widget with scrollable page list, type indicators, and search bar UI.

Purpose: Displays wiki pages in a scrollable sidebar with type icon/chip indicators and a search bar that triggers onQueryChanged callback per D-03 (Provider state management) and CONTEXT.md requirements.
Output: WikiPageList widget with type indicators and search bar
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modal-ui-components/02-CONTEXT.md
@packages/core/lib/models/wiki_page.dart
@packages/core/lib/models/wiki_page_type.dart
@packages/core/lib/services/wiki_search_service.dart
</context>

<interfaces>
From packages/core/lib/models/wiki_page.dart:
```dart
class WikiPage {
  final String id;
  final String title;
  final WikiPageType pageType;
  final String body;
  final List<String> tags;
  final List<String> aliases;
  // ...
}
```

From packages/core/lib/models/wiki_page_type.dart:
```dart
enum WikiPageType { creature, spell, item, rule, location, npc, other }
extension WikiPageTypeExtension on WikiPageType {
  bool get isReferenceType;
  String get displayName;
}
```

From packages/core/lib/services/wiki_search_service.dart:
```dart
class WikiSearchResult {
  final WikiPage page;
  final int score;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create WikiPageList widget with scrollable list and type indicators</name>
  <files>packages/core/lib/wiki/wiki_page_list.dart</files>
  <read_first>
    - packages/core/lib/models/wiki_page.dart (WikiPage model)
    - packages/core/lib/models/wiki_page_type.dart (WikiPageType enum + displayName)
    - apps/dm_app/lib/main.dart (existing _SidebarSection pattern for reference)
  </read_first>
  <action>
    Create packages/core/lib/wiki/wiki_page_list.dart with two widgets:

    1. WikiPageList (StatelessWidget):
       - Constructor: required List<WikiPage> pages, required ValueChanged<String> onQueryChanged, required ValueChanged<WikiPage> onPageSelected, String? selectedPageId
       - Contains a Column with:
         a. Search bar at top: TextField with decoration: InputDecoration(prefixIcon: Icon(Icons.search), hintText: 'Search wiki...', border: OutlineInputBorder), onChanged calls onQueryChanged
         b. Expanded ListView.builder below showing page list items
       - Each list item uses ListTile with:
         - leading: Icon based on pageType (creature->Icons.pets, spell->Icons.auto_awesome, item->Icons.inventory_2, rule->Icons.menu_book, location->Icons.place, npc->Icons.people, other->Icons.article)
         - title: Text(page.title)
         - trailing: Chip with page.pageType.displayName (text only, small size)
         - selected: page.id == selectedPageId
         - onTap: () => onPageSelected(page)

    2. Keep under 150 lines per CONTEXT.md constraint

    Use Material 3 theming (Theme.of(context)) for colors. Use VisualDensity.compact for list tiles.

    Icon mapping by WikiPageType:
    - creature: Icons.pets
    - spell: Icons.auto_awesome
    - item: Icons.inventory_2
    - rule: Icons.menu_book
    - location: Icons.place
    - npc: Icons.people
    - other: Icons.article
  </action>
  <acceptance_criteria>
    - packages/core/lib/wiki/wiki_page_list.dart exists
    - Contains WikiPageList class extending StatelessWidget
    - Constructor has pages (List<WikiPage>), onQueryChanged (ValueChanged<String>), onPageSelected (ValueChanged<WikiPage>), selectedPageId (String?) parameters
    - Contains TextField with search icon prefix and 'Search wiki...' hintText
    - TextField onChanged calls onQueryChanged
    - Contains ListView.builder rendering page list
    - Each ListTile has leading Icon mapped by pageType
    - Each ListTile has trailing Chip with pageType.displayName
    - ListTile selected state uses selectedPageId comparison
    - File is under 150 lines
    - `dart analyze packages/core` passes with no errors
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart analyze</automated>
  </verify>
  <done>WikiPageList created with search TextField, scrollable ListView, type icon indicators, type displayName chips, selected state, under 150 lines, dart analyze passes</done>
</task>

</tasks>

<verification>
- WikiPageList widget created in packages/core/lib/wiki/wiki_page_list.dart
- Search bar with TextField and onQueryChanged callback
- Type indicators using Icon + Chip with displayName
- Scrollable ListView for pages
- dart analyze passes
- File under 150 lines
</verification>

<success_criteria>
- WikiPageList renders scrollable list of WikiPage items
- Each item shows type-appropriate icon and displayName chip
- Search bar TextField triggers onQueryChanged callback on text input
- Selected page highlighted in list
- Widget under 150 lines
- dart analyze passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-modal-ui-components/02-02-SUMMARY.md`
</output>
