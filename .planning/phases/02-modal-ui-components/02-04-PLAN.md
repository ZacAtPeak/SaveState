---
phase: 02-modal-ui-components
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/lib/wiki/wiki_page_detail.dart
autonomous: true
requirements: [DETAIL-01, DETAIL-02, DETAIL-04]
must_haves:
  truths:
    - "Selected page renders markdown content with proper formatting"
    - "Tags displayed as chips in the detail view header"
    - "Detail view adapts to responsive layout (used in both two-panel and single-panel)"
  artifacts:
    - path: "packages/core/lib/wiki/wiki_page_detail.dart"
      provides: "Markdown rendering with tag chips header"
      exports: ["WikiPageDetail"]
  key_links:
    - from: "packages/core/lib/wiki/wiki_page_detail.dart"
      to: "flutter_markdown"
      via: "MarkdownBody widget"
      pattern: "MarkdownBody"
    - from: "packages/core/lib/wiki/wiki_page_detail.dart"
      to: "WikiPage.tags"
      via: "Wrap of Chip widgets"
      pattern: "page\\.tags\\.map.*Chip"
---

<objective>
Create the WikiPageDetail view with markdown rendering and tag chips in header.

Purpose: Renders selected wiki page content with flutter_markdown_plus formatting, displays tags as chips in the header, and adapts responsively per CONTEXT.md decisions D-03 (flutter_markdown) and D-04 (Provider state management).
Output: WikiPageDetail widget with markdown rendering and tag chips
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modal-ui-components/02-CONTEXT.md
@packages/core/lib/models/wiki_page.dart
@packages/core/lib/models/wiki_page_type.dart
</context>

<interfaces>
From packages/core/lib/models/wiki_page.dart:
```dart
class WikiPage {
  final String id;
  final String title;
  final WikiPageType pageType;
  final String body;
  final List<String> tags;
  final Map<String, dynamic> statBlock;
  // ...
}
```

From packages/core/lib/models/wiki_page_type.dart:
```dart
extension WikiPageTypeExtension on WikiPageType {
  String get displayName;  // 'Creature', 'Spell', etc.
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create WikiPageDetail widget with markdown rendering and tag chips</name>
  <files>packages/core/lib/wiki/wiki_page_detail.dart</files>
  <read_first>
    - packages/core/lib/models/wiki_page.dart (WikiPage model with body, tags, statBlock)
    - packages/core/lib/models/wiki_page_type.dart (displayName extension)
    - packages/core/pubspec.yaml (confirm flutter_markdown dependency added by Plan 02-01)
  </read_first>
  <action>
    Create packages/core/lib/wiki/wiki_page_detail.dart with:

    1. WikiPageDetail (StatelessWidget):
       - Constructor: required WikiPage? page
       - If page is null: return Center(child: Text('Select a page to view'))
       - If page is not null: return Column with:
         a. AppBar-like header (Padding with Container):
            - Title: Text(page.title, style: Theme text headline)
            - Below title: Wrap of Chips for each tag in page.tags
              - Each Chip: Chip(label: Text(tag), visualDensity: VisualDensity.compact)
            - Subtitle: Text(page.pageType.displayName, style: Theme text bodySmall)
         b. Expanded Markdown body:
            - Use MarkdownBody from flutter_markdown package (not Markdown widget — MarkdownBody is lighter, no scroll needed since parent is scrollable)
            - markdownBody: page.body
            - If page.body.isEmpty: Center(child: Text('No content'))

    2. Use Material 3 theming throughout (Theme.of(context))

    3. Keep under 150 lines per CONTEXT.md constraint

    Required import: package:flutter_markdown/flutter_markdown.dart

    Tag chips layout:
    ```dart
    if (page.tags.isNotEmpty)
      Wrap(
        spacing: 4,
        runSpacing: 4,
        children: page.tags.map((tag) => Chip(
          label: Text(tag, style: theme.textTheme.labelSmall),
          visualDensity: VisualDensity.compact,
          materialTapTargetSize: MaterialTapTargetSize.shrinkWrap,
        )).toList(),
      ),
    ```
  </action>
  <acceptance_criteria>
    - packages/core/lib/wiki/wiki_page_detail.dart exists
    - Contains WikiPageDetail class extending StatelessWidget
    - Constructor has page (WikiPage?) parameter
    - Shows 'Select a page to view' when page is null
    - Header displays page.title as headline text
    - Tags rendered as Wrap of Chip widgets
    - page.pageType.displayName shown as subtitle
    - MarkdownBody renders page.body content
    - Shows 'No content' when page.body is empty
    - File imports package:flutter_markdown/flutter_markdown.dart
    - File is under 150 lines
    - `dart analyze packages/core` passes with no errors
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart analyze</automated>
  </verify>
  <done>WikiPageDetail created with MarkdownBody rendering, tag chips in header, page type displayName, empty state handling, under 150 lines, dart analyze passes</done>
</task>

</tasks>

<verification>
- WikiPageDetail renders markdown content using flutter_markdown
- Tags displayed as chips in header area
- Empty page and null page states handled
- dart analyze passes
- File under 150 lines
</verification>

<success_criteria>
- WikiPageDetail renders markdown content with proper formatting via MarkdownBody
- Tags displayed as chips in header with Wrap layout
- Page type displayName shown below title
- Null page shows 'Select a page to view' placeholder
- Empty body shows 'No content' placeholder
- Widget under 150 lines
- dart analyze passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-modal-ui-components/02-04-SUMMARY.md`
</output>
