---
phase: 02-modal-ui-components
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - packages/core/lib/wiki/wiki_page_list.dart
autonomous: true
requirements: [LIST-01, LIST-04]
must_haves:
  truths:
    - "Search results prioritize title matches over body matches"
    - "Search input has debounce to prevent excessive re-filtering"
    - "Typing in search bar filters the visible page list"
  artifacts:
    - path: "packages/core/lib/wiki/wiki_page_list.dart"
      provides: "Debounced search filtering using WikiSearchService"
      contains: "Timer.*250"
  key_links:
    - from: "packages/core/lib/wiki/wiki_page_list.dart"
      to: "WikiSearchService.search()"
      via: "debounced query triggers search"
      pattern: "_searchService\\.search"
    - from: "packages/core/lib/wiki/wiki_page_list.dart"
      to: "Timer"
      via: "debounce mechanism"
      pattern: "Timer\\(Duration\\(milliseconds:\\s*250"
---

<objective>
Integrate search with title-prioritized filtering and debounce into the WikiPageList widget.

Purpose: Wire the search bar to WikiSearchService with 250ms debounce so search results prioritize title matches (score 10) over body matches (score 1) per CONTEXT.md requirements LIST-01 and LIST-04.
Output: WikiPageList with debounced search filtering using WikiSearchService
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modal-ui-components/02-CONTEXT.md
@packages/core/lib/services/wiki_search_service.dart
@packages/core/lib/models/wiki_page.dart
</context>

<interfaces>
From packages/core/lib/services/wiki_search_service.dart:
```dart
class WikiSearchResult {
  final WikiPage page;
  final int score;
}

class WikiSearchService {
  void index(List<WikiPage> pages);
  List<WikiSearchResult> search(String query);
  // score weights: title=10, body=1, tag=5
}
```

From packages/core/lib/models/wiki_page.dart:
```dart
class WikiPage {
  final String id;
  final String title;
  final WikiPageType pageType;
  final String body;
  final List<String> tags;
  // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert WikiPageList to StatefulWidget with debounced search</name>
  <files>packages/core/lib/wiki/wiki_page_list.dart</files>
  <read_first>
    - packages/core/lib/wiki/wiki_page_list.dart (current state from Plan 02-02)
    - packages/core/lib/services/wiki_search_service.dart (WikiSearchService interface)
  </read_first>
  <action>
    Modify packages/core/lib/wiki/wiki_page_list.dart:

    1. Convert WikiPageList from StatelessWidget to StatefulWidget

    2. Add to _WikiPageListState:
       - late final WikiSearchService _searchService
       - Timer? _debounceTimer
       - String _currentQuery = ''
       - List<WikiPage> get _displayedPages — returns filtered results

    3. In initState():
       - _searchService = WikiSearchService()
       - _searchService.index(widget.pages)

    4. In dispose():
       - _debounceTimer?.cancel()

    5. In _onQueryChanged(String query):
       - _debounceTimer?.cancel()
       - _debounceTimer = Timer(Duration(milliseconds: 250), () {
           setState(() { _currentQuery = query; });
         })
       - This implements 250ms debounce to prevent excessive re-filtering

    6. _displayedPages getter:
       - If _currentQuery.isEmpty: return widget.pages
       - Otherwise: return _searchService.search(_currentQuery).map((r) => r.page).toList()
       - WikiSearchService.search() already returns results sorted by score (title=10, body=1, tag=5)

    7. Update ListView.builder to use _displayedPages instead of widget.pages

    8. Pass _onQueryChanged to TextField onChanged instead of widget.onQueryChanged
       - Still call widget.onQueryChanged(query) for external notification

    9. Keep file under 150 lines per CONTEXT.md constraint

    Required imports: dart:async (for Timer), package:core/services/services.dart (for WikiSearchService)
  </action>
  <acceptance_criteria>
    - WikiPageList is StatefulWidget (not StatelessWidget)
    - File imports dart:async
    - File imports package:core/services/services.dart
    - _WikiPageListState has WikiSearchService field initialized in initState
    - _debounceTimer field exists and is cancelled in dispose
    - _onQueryChanged method uses Timer with Duration(milliseconds: 250)
    - _displayedPages getter calls _searchService.search() when query non-empty
    - ListView.builder uses _displayedPages
    - TextField onChanged calls _onQueryChanged
    - widget.onQueryChanged still called for external notification
    - File is under 150 lines
    - `dart analyze packages/core` passes with no errors
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart analyze</automated>
  </verify>
  <done>WikiPageList converted to StatefulWidget with 250ms debounced search using WikiSearchService, title-prioritized results (score 10/1/5), under 150 lines, dart analyze passes</done>
</task>

</tasks>

<verification>
- WikiPageList uses WikiSearchService for filtering
- 250ms debounce timer prevents excessive re-filtering
- Search results sorted by score (title matches prioritized)
- dart analyze passes
- File under 150 lines
</verification>

<success_criteria>
- Search input has 250ms debounce before triggering filter
- Search results use WikiSearchService title-prioritized scoring (title=10, tag=5, body=1)
- Empty query shows all pages
- Search results sorted by relevance score descending
- Widget under 150 lines
- dart analyze passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-modal-ui-components/02-03-SUMMARY.md`
</output>
