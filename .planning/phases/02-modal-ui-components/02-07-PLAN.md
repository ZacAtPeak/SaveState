---
phase: 02-modal-ui-components
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/lib/wiki/wiki_page_list.dart
autonomous: true
gap_closure: true
requirements:
  - LIST-04

must_haves:
  truths:
    - "Each list item shows a trailing Chip with the page type's display name"
    - "Type displayName is human-readable (e.g. 'Creature', 'Spell', not 'creature', 'spell')"
  artifacts:
    - path: "packages/core/lib/wiki/wiki_page_list.dart"
      provides: "ListTile with trailing Chip showing pageType.displayName"
      contains: "Chip(label: Text(page.pageType.displayName))"
  key_links:
    - from: "wiki_page_list.dart"
      to: "WikiPageType.displayName"
      via: "Chip label in ListTile trailing"
      pattern: "page\\.pageType\\.displayName"
---

<objective>
Add type displayName chips as trailing widgets on WikiPageList ListTile items.

Purpose: Close the partial verification gap — type icons exist but displayName chips are missing, reducing usability for identifying page types at a glance. Addresses LIST-04 requirement ("icon or chip" — icons alone only partially satisfy).
Output: WikiPageList with trailing type name Chip on each list item
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-modal-ui-components/02-VERIFICATION.md
@packages/core/lib/wiki/wiki_page_list.dart

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From wiki_page_list.dart (current ListTile at lines 74-81):
```dart
return ListTile(
  leading: Icon(_iconForType(page.pageType)),
  title: Text(page.title),
  subtitle: page.tags.isNotEmpty
      ? Text(page.tags.join(', '))
      : null,
  onTap: () => widget.onPageSelected?.call(page),
);
```

From WikiPageType enum (models/wiki_page_type.dart):
```dart
enum WikiPageType {
  creature,  // displayName: 'Creature'
  spell,     // displayName: 'Spell'
  item,      // displayName: 'Item'
  rule,      // displayName: 'Rule'
  location,  // displayName: 'Location'
  npc,       // displayName: 'NPC'
  other,     // displayName: 'Other'
}
// Has .displayName getter and .isReferenceType getter
```

Gap reason (from VERIFICATION.md): grep for `Chip|displayName` returns no matches in wiki_page_list.dart. Requirement LIST-04 says "type indicator (icon or chip)" — icons alone are partial.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trailing type displayName Chip to WikiPageList ListTile items</name>
  <files>packages/core/lib/wiki/wiki_page_list.dart</files>
  <action>
    In wiki_page_list.dart, modify the ListTile in the ListView.builder itemBuilder (lines 74-81):

    Add a `trailing` property to the ListTile with a Chip showing the page type's display name:
    ```dart
    return ListTile(
      leading: Icon(_iconForType(page.pageType)),
      title: Text(page.title),
      subtitle: page.tags.isNotEmpty
          ? Text(page.tags.join(', '))
          : null,
      trailing: Chip(
        label: Text(page.pageType.displayName),
        visualDensity: VisualDensity.compact,
        materialTapTargetSize: MaterialTapTargetSize.shrinkWrap,
      ),
      onTap: () => widget.onPageSelected?.call(page),
    );
    ```

    Use `VisualDensity.compact` and `MaterialTapTargetSize.shrinkWrap` to keep the chip small and not dominate the list item (consistent with the tag chips in WikiPageDetail).

    Verify the file stays under 150 lines per CONTEXT.md constraint (currently 107 lines, adding ~6 lines = ~113 lines).

    Reference existing code: The ListTile is at lines 74-81 in wiki_page_list.dart. Tag chips in wiki_page_detail.dart use `VisualDensity.compact` (line 27).

    Gap reason: VERIFICATION.md gap 3 — WikiPageList missing type displayName chips, only has icons.
  </action>
  <verify>
    <automated>cd packages/core && dart analyze lib/wiki/wiki_page_list.dart</automated>
  </verify>
  <done>Each ListTile in WikiPageList has trailing Chip with page.pageType.displayName, uses compact visual density, dart analyze passes, file under 150 lines</done>
</task>

</tasks>

<verification>
- `cd packages/core && dart analyze` passes with no issues
- grep for "page.pageType.displayName" in wiki_page_list.dart returns a match
- grep for "Chip" in wiki_page_list.dart returns a match (was previously empty)
- File stays under 150 lines
</verification>

<success_criteria>
- LIST-04 requirement fully satisfied (both icon AND chip present)
- VERIFICATION.md gap 3 closed (type displayName chips added)
- dart analyze passes on core package
</success_criteria>

<output>
After completion, create `.planning/phases/02-modal-ui-components/02-07-SUMMARY.md`
</output>
