---
phase: 01-core-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - packages/core/test/wiki_page_test.dart
  - packages/core/test/wiki_storage_service_test.dart
  - packages/core/test/wiki_search_service_test.dart
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04]

must_haves:
  truths:
    - "WikiPage serialization round-trips: toJson → fromJson produces identical values"
    - "WikiPageType enum serializes via .name and deserializes via values.byName()"
    - "WikiStorageService saves a page and loads it back with identical data"
    - "WikiStorageService lists all pages in the directory"
    - "WikiStorageService deletes a page file"
    - "WikiSearchService returns title matches ranked above body matches"
    - "WikiSearchService returns empty results for non-matching queries"
  artifacts:
    - path: "packages/core/test/wiki_page_test.dart"
      provides: "Unit tests for WikiPage and WikiPageType serialization"
      contains: "test("
    - path: "packages/core/test/wiki_storage_service_test.dart"
      provides: "Unit tests for WikiStorageService save/load/list/delete"
      contains: "test("
    - path: "packages/core/test/wiki_search_service_test.dart"
      provides: "Unit tests for WikiSearchService indexing and search scoring"
      contains: "test("
  key_links:
    - from: "packages/core/test/wiki_page_test.dart"
      to: "packages/core/lib/models/wiki_page.dart"
      via: "import and serialization testing"
      pattern: "WikiPage.fromJson.*toJson"
    - from: "packages/core/test/wiki_storage_service_test.dart"
      to: "packages/core/lib/services/wiki_storage_service.dart"
      via: "import and I/O testing with temp directory"
      pattern: "WikiStorageService"
    - from: "packages/core/test/wiki_search_service_test.dart"
      to: "packages/core/lib/services/wiki_search_service.dart"
      via: "import and search scoring testing"
      pattern: "WikiSearchService"
---

<objective>
Create core package unit tests for WikiPage model serialization, WikiStorageService persistence, and WikiSearchService search scoring.

Purpose: The core package has zero test coverage (per CONCERNS.md). Phase 1 must establish the test directory and provide tests for all new code. Tests verify that models serialize correctly, storage persists and retrieves data, and search ranking works as specified.

Output: Three test files in packages/core/test/, test directory created, all tests passing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/01-core-infrastructure/01-01-PLAN.md
@.planning/phases/01-core-infrastructure/01-02-PLAN.md
@.planning/phases/01-core-infrastructure/01-03-PLAN.md
@packages/core/lib/models/wiki_page.dart
@packages/core/lib/models/wiki_page_type.dart
@packages/core/lib/services/wiki_storage_service.dart
@packages/core/lib/services/wiki_search_service.dart
@packages/core/pubspec.yaml
</context>

<interfaces>
<!-- Key types from Plans 01-03 that tests will verify. -->

From packages/core/lib/models/wiki_page.dart:
```dart
class WikiPage {
  final String id;
  final String title;
  final WikiPageType pageType;
  final String body;
  final List<String> tags;
  final List<String> aliases;
  final DateTime createdAt;
  final DateTime updatedAt;
  final String? referenceId;
  final Map<String, dynamic> statBlock;
  Map<String, dynamic> toJson();
  factory WikiPage.fromJson(Map<String, dynamic> json);
}
```

From packages/core/lib/services/wiki_storage_service.dart:
```dart
class WikiStorageService {
  WikiStorageService({required Directory baseDirectory});
  Future<void> savePage(WikiPage page);
  Future<WikiPage?> loadPage(String pageId);
  Future<List<WikiPage>> loadAllPages();
  Future<void> deletePage(String pageId);
}
```

From packages/core/lib/services/wiki_search_service.dart:
```dart
class WikiSearchResult {
  final WikiPage page;
  final int score;
}
class WikiSearchService {
  void index(List<WikiPage> pages);
  void addPage(WikiPage page);
  void removePage(String pageId);
  void clear();
  List<WikiSearchResult> search(String query);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test directory and WikiPage serialization tests</name>
  <files>packages/core/test/wiki_page_test.dart</files>
  <action>
    1. Create `packages/core/test/` directory (does not exist yet, per CONCERNS.md).

    2. Create `packages/core/test/wiki_page_test.dart` with tests:

    Test WikiPageType enum:
    - 'has 7 values' — expect(WikiPageType.values.length, equals(7))
    - 'creature isReferenceType is true' — expect(WikiPageType.creature.isReferenceType, isTrue)
    - 'npc isReferenceType is true' — expect(WikiPageType.npc.isReferenceType, isTrue)
    - 'spell isReferenceType is false' — expect(WikiPageType.spell.isReferenceType, isFalse)
    - 'serialization round-trip' — WikiPageType.spell.name → WikiPageType.values.byName('spell') → equals(WikiPageType.spell)

    Test WikiPage creation:
    - 'generates UUID when id not provided' — create page, expect page.id.isNotEmpty, isA<valid UUID format>
    - 'uses provided id when given' — create with id: 'test-123', expect page.id == 'test-123'
    - 'sets default values' — create with only required fields, expect body == '', tags.isEmpty, aliases.isEmpty, statBlock.isEmpty

    Test WikiPage JSON serialization:
    - 'toJson includes all fields' — create a full page, call toJson, expect all keys present
    - 'fromJson reconstructs identical page' — create page → toJson → fromJson → expect all fields match
    - 'serializes pageType as enum name' — expect json['pageType'] == 'creature'
    - 'serializes DateTime as ISO string' — expect json['createdAt'] is a valid ISO string
    - 'deserializes pageType from enum name' — fromJson with 'pageType': 'spell' → page.pageType == WikiPageType.spell
    - 'handles nullable referenceId' — page with referenceId null → toJson has null → fromJson has null
    - 'handles statBlock map' — page with statBlock {'level': 3, 'school': 'evocation'} → round-trips correctly

    Import: `package:test/test.dart` and `package:core/models/models.dart`.

    Use `group()` to organize tests by concern (WikiPageType, WikiPage creation, WikiPage serialization).
  </action>
  <verify>
    <automated>cd packages/core && dart test test/wiki_page_test.dart</automated>
  </verify>
  <done>Test directory created, wiki_page_test.dart has tests for enum, creation, and serialization. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create WikiStorageService persistence tests</name>
  <files>packages/core/test/wiki_storage_service_test.dart</files>
  <action>
    Create `packages/core/test/wiki_storage_service_test.dart` with tests:

    Use `Directory.systemTemp.createTemp('wiki_test_')` to create a temporary directory for each test, ensuring tests don't interfere with real data. Clean up in `tearDown()` with `tempDir.delete(recursive: true)`.

    Test savePage:
    - 'saves page as JSON file' — create service with temp dir, save a page, expect File('${tempDir.path}/wiki/pages/${page.id}.json').existsSync() is true
    - 'saved file contains valid JSON' — read the file, jsonDecode it, expect it's a Map with 'title' key

    Test loadPage:
    - 'loads a saved page with identical data' — save page, load it back, expect loaded.title == original.title, loaded.pageType == original.pageType, etc.
    - 'returns null for non-existent page' — loadPage('nonexistent-id'), expect null

    Test loadAllPages:
    - 'returns all saved pages' — save 3 pages, loadAllPages, expect length == 3
    - 'returns empty list when no pages exist' — new temp dir, loadAllPages, expect isEmpty

    Test deletePage:
    - 'removes the page file' — save page, delete it, expect file no longer exists
    - 'page not in loadAllPages after delete' — save 2 pages, delete 1, loadAllPages, expect length == 1

    Import: `package:test/test.dart`, `package:core/models/models.dart`, `package:core/services/services.dart`, `dart:io`, `dart:convert`.
  </action>
  <verify>
    <automated>cd packages/core && dart test test/wiki_storage_service_test.dart</automated>
  </verify>
  <done>WikiStorageService tests for save/load/list/delete all pass with temp directory isolation.</done>
</task>

<task type="auto">
  <name>Task 3: Create WikiSearchService scoring tests</name>
  <files>packages/core/test/wiki_search_service_test.dart</files>
  <action>
    Create `packages/core/test/wiki_search_service_test.dart` with tests:

    Test indexing:
    - 'index replaces existing pages' — add page A, index with page B, search returns only B
    - 'addPage adds to index' — index with page A, addPage page B, search returns both
    - 'removePage removes from index' — index with pages A and B, removePage A, search returns only B
    - 'clear empties index' — index with pages, clear, search returns empty

    Test search scoring:
    - 'title match scores higher than body match' — create page with title "Fireball" and body "a spell", search "fireball" → score > search "spell" on a page with title "Spells" and body "fireball description"
    - 'returns results sorted by score' — create 3 pages with varying match quality, search, expect results in descending score order
    - 'empty query returns all pages' — index 3 pages, search(''), expect 3 results
    - 'non-matching query returns empty' — search for "xyznonexistent", expect isEmpty
    - 'case-insensitive matching' — page title "Fireball", search "fireball" → found, search "FIREBALL" → found
    - 'multi-word query accumulates scores' — page title "Fire Bolt", search "fire bolt" → score higher than searching just "fire"
    - 'tag matches contribute to score' — page with tags ['evocation'], search "evocation" → found with score > 0

    Import: `package:test/test.dart`, `package:core/models/models.dart`, `package:core/services/services.dart`.

    Helper function to create test pages quickly:
    ```dart
    WikiPage _page({required String title, String body = '', List<String> tags = const []}) {
      return WikiPage(title: title, pageType: WikiPageType.spell, body: body, tags: tags);
    }
    ```
  </action>
  <verify>
    <automated>cd packages/core && dart test test/wiki_search_service_test.dart</automated>
  </verify>
  <done>WikiSearchService tests for indexing and search scoring all pass.</done>
</task>

</tasks>

<verification>
- All three test files exist in packages/core/test/
- `cd packages/core && dart test` runs all tests with no failures
- Tests cover: enum values, model creation, JSON round-trip, storage CRUD, search scoring
- Temporary directories used for storage tests (no side effects)
</verification>

<success_criteria>
- packages/core/test/ directory exists
- wiki_page_test.dart: 10+ tests covering WikiPageType and WikiPage serialization
- wiki_storage_service_test.dart: 7+ tests covering save/load/list/delete
- wiki_search_service_test.dart: 10+ tests covering indexing and search scoring
- All tests pass via `dart test` in packages/core
- Core package has test coverage for all Phase 1 code
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-04-SUMMARY.md`
</output>
