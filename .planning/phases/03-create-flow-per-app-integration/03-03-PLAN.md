---
phase: 03-create-flow-per-app-integration
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - apps/companion_app/lib/main.dart
  - apps/dm_app/lib/main.dart
  - apps/companion_app/test/wiki_entry_integration_test.dart
  - apps/dm_app/test/wiki_entry_integration_test.dart
autonomous: true
requirements: [CREATE-01]
must_haves:
  truths:
    - "Both apps expose a book icon in AppBar to open wiki modal"
    - "Both apps provide top-level WikiProvider that loads pages at startup"
    - "Opening modal in either app uses shared core create-enabled wiki UI"
  artifacts:
    - path: apps/companion_app/lib/main.dart
      provides: companion app AppBar wiki trigger + WikiProvider root wiring
    - path: apps/dm_app/lib/main.dart
      provides: dm app AppBar wiki trigger + WikiProvider root wiring
    - path: apps/companion_app/test/wiki_entry_integration_test.dart
      provides: companion app modal-launch verification
    - path: apps/dm_app/test/wiki_entry_integration_test.dart
      provides: dm app modal-launch verification
  key_links:
    - from: apps/companion_app/lib/main.dart
      to: packages/core/lib/wiki/wiki_modal_shell.dart
      via: AppBar IconButton onPressed
      pattern: "Icons\.menu_book"
    - from: apps/dm_app/lib/main.dart
      to: packages/core/lib/wiki/wiki_provider.dart
      via: top-level ChangeNotifierProvider
      pattern: "ChangeNotifierProvider"
---

<objective>
Wire the shared wiki modal entrypoint and top-level wiki state provider into both Flutter apps.

Purpose: Deliver D-08 and D-09 in production entry points so create flow and page updates are available from both app shells.
Output: app root/provider integration, AppBar wiki trigger handlers, and app-level integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-create-flow-per-app-integration/03-CONTEXT.md
@.planning/phases/03-create-flow-per-app-integration/03-RESEARCH.md
@.planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md
@apps/companion_app/lib/main.dart
@apps/dm_app/lib/main.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing app integration tests for wiki entrypoint in both apps (D-08, D-09)</name>
  <files>apps/companion_app/test/wiki_entry_integration_test.dart, apps/dm_app/test/wiki_entry_integration_test.dart</files>
  <read_first>
    - apps/companion_app/lib/main.dart
    - apps/dm_app/lib/main.dart
    - .planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md (`apps/*/main.dart` + `New tests in packages/core/test/` analog style)
    - .planning/phases/03-create-flow-per-app-integration/03-UI-SPEC.md
  </read_first>
  <action>
    Create widget/integration tests that fail initially and assert each app has `Icons.menu_book` in AppBar, tapping it opens shared wiki modal, and app tree contains top-level `WikiProvider` loaded at startup. Follow 03-PATTERNS.md analog mappings for test organization (group/test structure and deterministic setup style).
  </action>
  <acceptance_criteria>
    - Two test files exist (one per app)
    - Tests validate icon presence, tap behavior, and provider-root wiring
    - Tests fail before implementation changes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/companion_app && flutter test test/wiki_entry_integration_test.dart && cd ../dm_app && flutter test test/wiki_entry_integration_test.dart</automated>
  </verify>
  <done>Executable failing tests define both-app entrypoint behavior contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire top-level WikiProvider and AppBar book icon triggers in both apps (D-08, D-09)</name>
  <files>apps/companion_app/lib/main.dart, apps/dm_app/lib/main.dart</files>
  <read_first>
    - apps/companion_app/lib/main.dart
    - apps/dm_app/lib/main.dart
    - packages/core/lib/wiki/wiki_modal_shell.dart
    - packages/core/lib/wiki/wiki_provider.dart
    - .planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md (`apps/*/main.dart` AppBar action + `wiki_modal_shell.dart` invocation contract)
  </read_first>
  <action>
    Update both app roots to wrap `MaterialApp` in a top-level `ChangeNotifierProvider<WikiProvider>` that loads all wiki pages at startup (per D-09). Add/ensure `Icons.menu_book` AppBar action in both apps and wire `onPressed` to open `WikiModalShell.show(...)` with provider-backed page state (per D-08). Apply the 03-PATTERNS.md app-shell analog mapping: retain existing MaterialApp root structure, AppBar IconButton convention, and modal invocation contract.
  </action>
  <acceptance_criteria>
    - Both apps have top-level WikiProvider wiring around MaterialApp
    - Startup load path calls provider load method once
    - AppBar wiki action exists in companion_app and dm_app
    - Tapping wiki action opens modal using shared core wiki UI
  </acceptance_criteria>
  <verify>
    <automated>cd apps/companion_app && flutter test test/wiki_entry_integration_test.dart && cd ../dm_app && flutter test test/wiki_entry_integration_test.dart</automated>
  </verify>
  <done>Both apps expose consistent wiki modal entry and shared provider-backed state.</done>
</task>

<task type="auto">
  <name>Task 3: Run cross-workspace verification for app/core integration stability</name>
  <files>apps/companion_app/lib/main.dart, apps/dm_app/lib/main.dart, apps/companion_app/test/wiki_entry_integration_test.dart, apps/dm_app/test/wiki_entry_integration_test.dart</files>
  <read_first>
    - pubspec.yaml
  </read_first>
  <action>
    Execute workspace-level verification to ensure no integration regressions between core wiki package changes and both apps. Fix import/provider wiring issues found by analyzer/tests in this same task.
  </action>
  <acceptance_criteria>
    - Companion app tests pass
    - DM app tests pass
    - Core package analysis/tests remain green from prior plans
  </acceptance_criteria>
  <verify>
    <automated>cd apps/companion_app && flutter test test/wiki_entry_integration_test.dart</automated>
  </verify>
  <done>Both apps and shared core pass automated validation with create-flow entry integrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App shell actions → modal route invocation | User taps trigger route transitions and provider-backed data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | S | app main.dart AppBar action | mitigate | Use explicit icon/tooltip and direct handler to trusted WikiModalShell entrypoint only |
| T-03-08 | D | app startup provider load | mitigate | Load pages once at startup and avoid repeated reload loops during rebuilds |
| T-03-09 | R | both-app modal entry | mitigate | Add app-level tests proving tap-to-modal flow remains consistent in both apps |
</threat_model>

<verification>
- `cd apps/companion_app && flutter test test/wiki_entry_integration_test.dart`
- `cd apps/dm_app && flutter test test/wiki_entry_integration_test.dart`
- `cd apps/companion_app && flutter test test/wiki_entry_integration_test.dart` (task-level smoke feedback, target <=30s)
- `dart test && cd apps/companion_app && flutter test && cd ../dm_app && flutter test` (wave/phase gate)
</verification>

<success_criteria>
- Locked decisions D-08 and D-09 are fully implemented in companion and dm app entrypoints.
- Both apps can launch the shared create-enabled wiki modal from AppBar book icon.
</success_criteria>

<output>
After completion, create `.planning/phases/03-create-flow-per-app-integration/03-create-flow-per-app-integration-03-SUMMARY.md`
</output>
