---
phase: 03-create-flow-per-app-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/lib/models/wiki_page_type.dart
  - packages/core/lib/wiki/wiki_modal_provider.dart
  - packages/core/lib/wiki/wiki_modal_shell.dart
  - packages/core/lib/wiki/wiki_type_picker.dart
  - packages/core/lib/wiki/wiki_create_form.dart
  - packages/core/lib/wiki/wiki.dart
autonomous: true
requirements: [CREATE-01, CREATE-02, CREATE-03]
must_haves:
  truths:
    - "User can tap + in modal and reach type picker"
    - "Type picker shows all wiki page types in a 2x4 grid"
    - "Selecting a type opens a schema-driven form with type-specific fields"
    - "User can cancel create flow and return without creating a page"
  artifacts:
    - path: packages/core/lib/models/wiki_page_type.dart
      provides: WikiPageType field schema metadata getter
    - path: packages/core/lib/wiki/wiki_type_picker.dart
      provides: 2x4 icon-card type picker UI
    - path: packages/core/lib/wiki/wiki_create_form.dart
      provides: dynamic per-type form shell with shared + structured fields
  key_links:
    - from: packages/core/lib/wiki/wiki_modal_shell.dart
      to: packages/core/lib/wiki/wiki_type_picker.dart
      via: plus button create-mode entry
      pattern: "Icons.add"
    - from: packages/core/lib/wiki/wiki_create_form.dart
      to: packages/core/lib/models/wiki_page_type.dart
      via: selectedType.fields iteration
      pattern: "selectedType\.fields"
---

<objective>
Implement the create-entry UI state machine and schema-driven type/form rendering in shared core wiki components.

Purpose: Deliver D-01, D-03, D-04, D-05, and D-06 with deterministic modal behavior and centralized field-schema ownership.
Output: Type schema definitions, create-mode modal routing, type picker widget, and dynamic create form scaffold.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-create-flow-per-app-integration/03-CONTEXT.md
@.planning/phases/03-create-flow-per-app-integration/03-RESEARCH.md
@.planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md
@.planning/phases/03-create-flow-per-app-integration/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add centralized per-type field schemas in core model (D-06, D-05)</name>
  <files>packages/core/lib/models/wiki_page_type.dart</files>
  <read_first>
    - packages/core/lib/models/wiki_page_type.dart
    - .planning/phases/03-create-flow-per-app-integration/03-CONTEXT.md
  </read_first>
  <action>
    Extend `WikiPageTypeExtension` with a `fields` getter per D-06 that returns a typed list of field definitions (label, key, input type, required, optional hint/options). Include full per-type structured fields per D-05 for all 7 enum values. Keep schema ownership exclusively in `core/models` and do not hardcode field maps in widgets.
  </action>
  <acceptance_criteria>
    - `WikiPageTypeExtension.fields` exists for all WikiPageType values
    - Field definitions include stable key strings for statBlock mapping
    - Input-type metadata supports text/number/multiline/select render contracts
    - No per-type schema literals are introduced in widget files
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart analyze && dart test test/wiki_page_test.dart</automated>
  </verify>
  <done>Centralized schema metadata compiles and is consumable by create UI without duplicated type logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement create-flow navigation + type picker transitions (D-01, D-03, D-04, CREATE-01/02)</name>
  <files>packages/core/lib/wiki/wiki_modal_provider.dart, packages/core/lib/wiki/wiki_modal_shell.dart, packages/core/lib/wiki/wiki_type_picker.dart, packages/core/lib/wiki/wiki.dart</files>
  <read_first>
    - packages/core/lib/wiki/wiki_modal_provider.dart
    - packages/core/lib/wiki/wiki_modal_shell.dart
    - packages/core/lib/wiki/wiki_page_list.dart
    - .planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md (`wiki_type_picker.dart` no-analog fallback and modal/list analog references)
    - .planning/phases/03-create-flow-per-app-integration/03-UI-SPEC.md
  </read_first>
  <action>
    Add modal-state fields/methods (`isCreating`, `pendingType`, create start/cancel/select transitions) and wire `WikiModalShell` AppBar plus button to enter picker state, following `wiki_modal_provider.dart` and `wiki_modal_shell.dart` analog mutation/render patterns from 03-PATTERNS.md. Build `wiki_type_picker.dart` as a 2x4 icon-card grid from `WikiPageType.values` with displayName labels per D-03 and required cancel/back affordance per D-04. Because `wiki_type_picker.dart` is marked "No Analog Found" in 03-PATTERNS.md, use the documented fallback: enum-driven selector behavior and icon/text row conventions from `wiki_page_list.dart` as the nearest analog. Enforce D-01 routing: in two-panel mode render picker/form in right panel slot; in single-panel mode push create flow via Navigator and return to prior view on cancel.
  </action>
  <acceptance_criteria>
    - Modal AppBar includes `Icons.add` action with create semantics
    - Type picker renders 7 type cards from enum values in 2x4 layout
    - Card tap sets pending type and advances to form state
    - Cancel/back exits create flow without writing data
    - Two-panel and single-panel behavior follow D-01 exactly
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart analyze</automated>
  </verify>
  <done>Create flow entry and picker routing work in both responsive modes with cancel path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement dynamic create form rendering from schemas (D-05, D-06, CREATE-03)</name>
  <files>packages/core/lib/wiki/wiki_create_form.dart, packages/core/lib/wiki/wiki_modal_shell.dart, packages/core/lib/wiki/wiki.dart</files>
  <read_first>
    - packages/core/lib/models/wiki_page_type.dart
    - packages/core/lib/wiki/wiki_page_detail.dart
    - .planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md (`wiki_create_form.dart` analogs: `wiki_page_detail.dart` + storage contract)
    - .planning/phases/03-create-flow-per-app-integration/03-UI-SPEC.md
  </read_first>
  <action>
    Create `wiki_create_form.dart` rendering shared fields (title/body/tags/aliases) plus type-specific controls generated from `selectedType.fields`. Follow 03-PATTERNS.md analog mapping: use `wiki_page_detail.dart` sectioning/layout and `wiki_storage_service.dart` value-shape conventions so field keys/types match later persistence. Implement validator wiring for required fields and per-input typing (text/number/multiline/select). Keep this task focused on render + validation contract only; persistence is handled in Plan 03-02.
  </action>
  <acceptance_criteria>
    - Form reflects selected type field schema without hardcoded per-type branches
    - Required schema fields block submit until valid
    - Shared fields and structured fields are both present
    - Widget exports are updated via wiki barrel
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart analyze</automated>
  </verify>
  <done>Dynamic schema-driven form is visible and validates correctly for each page type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → create form validators | Untrusted text and numeric input crosses into domain object assembly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | wiki_create_form.dart | mitigate | Enforce schema-based required validation and numeric parsing before data leaves form state |
| T-03-02 | D | wiki_type_picker.dart | mitigate | Keep bounded grid with fixed 7 enum values; no unbounded dynamic list rendering |
| T-03-03 | R | wiki_modal_shell.dart | mitigate | Preserve explicit cancel/back transitions so create state exits are intentional and reproducible |
</threat_model>

<verification>
- `cd packages/core && dart analyze`
- `cd packages/core && dart test`
</verification>

<success_criteria>
- CREATE-01, CREATE-02, and CREATE-03 UI entry/routing contracts are implemented in shared core wiki components.
- Locked decisions D-01, D-03, D-04, D-05, D-06 are directly implemented by tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-create-flow-per-app-integration/03-create-flow-per-app-integration-01-SUMMARY.md`
</output>
