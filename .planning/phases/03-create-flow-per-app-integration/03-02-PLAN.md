---
phase: 03-create-flow-per-app-integration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/core/lib/wiki/wiki_create_form.dart
  - packages/core/lib/wiki/wiki_modal_provider.dart
  - packages/core/lib/wiki/wiki_provider.dart
  - packages/core/lib/services/wiki_storage_service.dart
  - packages/core/test/wiki_create_submit_test.dart
autonomous: true
requirements: [CREATE-04, CREATE-03]
must_haves:
  truths:
    - "Submitting a valid form persists a new page"
    - "Structured fields are saved into statBlock keys"
    - "New page appears immediately in sidebar data source"
    - "Newly created page is auto-selected and shown in detail"
  artifacts:
    - path: packages/core/lib/wiki/wiki_provider.dart
      provides: app-scope wiki pages state and load/save integration
    - path: packages/core/lib/wiki/wiki_create_form.dart
      provides: submit pipeline from validated form to persistence
    - path: packages/core/test/wiki_create_submit_test.dart
      provides: automated validation of save + select behavior
  key_links:
    - from: packages/core/lib/wiki/wiki_create_form.dart
      to: packages/core/lib/services/wiki_storage_service.dart
      via: savePage call
      pattern: "savePage\("
    - from: packages/core/lib/wiki/wiki_create_form.dart
      to: packages/core/lib/wiki/wiki_modal_provider.dart
      via: add/select transition
      pattern: "selectPage\("
---

<objective>
Implement validated submit/persist behavior for created pages and immediate in-memory refresh/select transitions.

Purpose: Deliver D-02, D-07, and D-09 plus CREATE-04 by completing the end-to-end save path from form to provider state.
Output: Save pipeline, app-scope WikiProvider, and automated submit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-create-flow-per-app-integration/03-CONTEXT.md
@.planning/phases/03-create-flow-per-app-integration/03-RESEARCH.md
@.planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md
@packages/core/lib/models/wiki_page.dart
@packages/core/lib/services/wiki_storage_service.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing submit-flow tests for persistence and auto-select (D-02, D-07, D-09)</name>
  <files>packages/core/test/wiki_create_submit_test.dart</files>
  <read_first>
    - packages/core/test/wiki_storage_service_test.dart
    - packages/core/lib/models/wiki_page.dart
    - .planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md (`New tests in packages/core/test/` analog + `wiki_create_form.dart` analog)
    - .planning/phases/03-create-flow-per-app-integration/03-CONTEXT.md
  </read_first>
  <action>
    Create test coverage that fails initially and asserts: valid create payload persists via storage, structured fields map into `statBlock` using schema keys (D-07), page list source updates immediately, and newly created page becomes selected detail target after save (D-02, D-09). Follow the 03-PATTERNS.md test analog mapping (`wiki_storage_service_test.dart` fixture lifecycle + async assertion shape) for test structure.
  </action>
  <acceptance_criteria>
    - Test file exists with explicit cases for save, statBlock mapping, list refresh, and auto-select
    - Tests fail before implementation and document expected output contract
    - No manual verification required for submit contract
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart test test/wiki_create_submit_test.dart -r compact</automated>
  </verify>
  <done>Executable failing tests define CREATE-04 behavior contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement submit pipeline with statBlock mapping and auto-select (D-02, D-07, CREATE-04)</name>
  <files>packages/core/lib/wiki/wiki_create_form.dart, packages/core/lib/wiki/wiki_modal_provider.dart</files>
  <read_first>
    - packages/core/lib/wiki/wiki_create_form.dart
    - packages/core/lib/wiki/wiki_modal_provider.dart
    - packages/core/lib/services/wiki_storage_service.dart
    - .planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md (`wiki_create_form.dart` + `wiki_modal_provider.dart` pattern assignments)
  </read_first>
  <action>
    Implement form submit flow: validate and normalize shared fields, map structured field values into `WikiPage.statBlock[field.key]` per D-07, persist new page via `WikiStorageService.savePage`, append to modal/provider page list, and auto-select new page then transition to detail view per D-02. Apply 03-PATTERNS.md analog mappings: use `wiki_page_detail.dart` layout conventions and `wiki_modal_provider.dart` private-field/getter/notifyListeners mutation style.
  </action>
  <acceptance_criteria>
    - New WikiPage includes title/body/tags/aliases/pageType/statBlock from form
    - Save operation uses WikiStorageService and handles async errors with user feedback
    - Post-save state sets selected page to newly created page
    - Create mode exits to detail view after successful save
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart test test/wiki_create_submit_test.dart -r compact</automated>
  </verify>
  <done>Valid submissions persist and immediately show newly created page in detail state.</done>
</task>

<task type="auto">
  <name>Task 3: Add app-scope WikiProvider load/list API and wire tests green (D-09)</name>
  <files>packages/core/lib/wiki/wiki_provider.dart, packages/core/lib/services/wiki_storage_service.dart, packages/core/test/wiki_create_submit_test.dart</files>
  <read_first>
    - packages/core/lib/wiki/wiki_modal_provider.dart
    - packages/core/lib/services/wiki_storage_service.dart
    - .planning/phases/03-create-flow-per-app-integration/03-PATTERNS.md (`wiki_provider.dart` provider + storage analog mapping)
  </read_first>
  <action>
    Implement `WikiProvider` (ChangeNotifier) that owns canonical pages list, loads all pages at startup, and provides add/save mutation APIs used by modal create flow. Keep storage API compatibility; only adjust service signatures if required for provider integration and update tests accordingly.
  </action>
  <acceptance_criteria>
    - WikiProvider exposes read-only pages getter and async load/add methods
    - Loading pulls from WikiStorageService once and notifies listeners
    - Submit tests pass against provider-owned pages list behavior
  </acceptance_criteria>
  <verify>
    <automated>cd packages/core && dart analyze</automated>
  </verify>
  <done>Provider-backed in-memory wiki list is authoritative and stays in sync with create submissions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form values → WikiPage persistence | Untrusted user input crosses into durable JSON storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | wiki_create_form.dart | mitigate | Normalize/validate per-field types before writing to statBlock and persisting |
| T-03-05 | D | wiki_storage_service.dart | mitigate | Keep async save path bounded; prevent duplicate submissions while save is in-flight |
| T-03-06 | I | wiki_provider.dart | accept | Local single-user file storage; no network exfiltration in this phase scope |
</threat_model>

<verification>
- `cd packages/core && dart test test/wiki_create_submit_test.dart -r compact`
- `cd packages/core && dart analyze` (task-level smoke feedback, target <=30s)
- `cd packages/core && dart test` (wave/phase gate)
</verification>

<success_criteria>
- CREATE-04 is fully covered with automated tests.
- Locked decisions D-02, D-07, and D-09 are implemented in save + selection + provider state ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/03-create-flow-per-app-integration/03-create-flow-per-app-integration-02-SUMMARY.md`
</output>
