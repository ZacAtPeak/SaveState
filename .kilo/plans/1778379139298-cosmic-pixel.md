# Plan: Create AGENTS.md

## Research Summary
Explored: README, package.json, tsconfig, vite.config, tauri.conf.json, Cargo.toml, Rust src files, capabilities, index.html, main.ts.

**Repo profile**: Simple Tauri v2 app — vanilla TS frontend + minimal Rust backend. No tests, no lint config, no CI.

## Key findings to encode

1. **Commands**:
   - `npm run tauri dev` — runs full app (internally triggers `npm run dev` via `beforeDevCommand`)
   - `npm run tauri build` — production build (internally triggers `npm run build`)
   - `npm run dev` — vite frontend only on port 1420
   - `npm run build` — `tsc && vite build` (typecheck then bundle)

2. **Tauri dev host quirk**: `vite.config.ts` reads `TAURI_DEV_HOST` env var to configure HMR for remote device development (ws protocol, port 1421). Most devs won't need this.

3. **Rust lib naming**: Cargo.toml declares `name = "save_state_lib"` with `_lib` suffix. Comment explicitly notes this is required for Windows uniqueness — do not remove.

4. **Vite ignores src-tauri**: `vite.config.ts` has `server.watch.ignored: ["**/src-tauri/**"]` — vite won't hot-reload Rust side changes.

5. **No test/lint tooling**: No Vitest, no ESLint, no pre-commit hooks. Strict TypeScript is the only code quality tool.

6. **Build order matters for full Tauri**: `beforeBuildCommand` runs `npm run build` (tsc first), so type errors will block Tauri builds.

## Proposed AGENTS.md (draft)

```
# save-state

Tauri v2 + Vite + vanilla TypeScript + Rust app.

## Dev commands

- `npm run tauri dev` — full app (frontend on :1420, Rust backend). Runs `npm run dev` automatically.
- `npm run tauri build` — production bundle. Runs `npm run build` (tsc → vite) first.
- `npm run dev` — Vite dev server only (no Rust).
- `npm run build` — typecheck (tsc) + Vite production build.

## Architecture

- `src/` — TypeScript frontend entrypoint (`main.ts`), assets, styles.
- `src-tauri/src/lib.rs` — Rust command handler (`greet`). Tauri entry via `save_state_lib::run()`.
- `index.html` — Web entry loaded by Tauri window.

## Notable quirks

- Rust lib name is `save_state_lib` (with `_lib` suffix). Required for Windows Cargo uniqueness — do not change.
- Vite config (`vite.config.ts`) ignores `src-tauri/` from file watching — Rust changes require restart.
- `TAURI_DEV_HOST` env var (set in shell) controls HMR remote device settings in vite.config.ts.
- No test framework or ESLint configured. TypeScript strict mode is the only code quality gate.
- `beforeBuildCommand` runs `tsc` — type errors will block `npm run tauri build`.
```

## Verification
- All commands verified against package.json scripts and tauri.conf.json
- Rust lib naming confirmed via Cargo.toml comment explaining Windows workaround
- Vite HMR config details confirmed in vite.config.ts
- No existing AGENTS.md found